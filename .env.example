# MongoDB connection string (local or Atlas)
DATABASE_URL="mongodb://localhost:27017/hotflow"

# JWT secret — generate with: openssl rand -hex 32
JWT_SECRET="change-me-to-a-random-64-char-hex-string"

# JWT expiration
JWT_EXPIRES_IN="7d"
